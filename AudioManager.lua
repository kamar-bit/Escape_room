-- ════════════════════════════════════════════
--  AudioManager.lua
-- ════════════════════════════════════════════
local AudioManager = {}

function AudioManager.playEffect(name)
    -- placeholder: generate beep via SoundData if needed
    -- love.audio.newSource(...)
end

function AudioManager.playAmbient(name) end
function AudioManager.playRhythm(n) end

return AudioManager
